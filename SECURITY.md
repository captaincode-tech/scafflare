# سیاست امنیتی

## نسخه‌های پشتیبانی‌شده

فقط آخرین نسخهٔ minor منتشرشده از خط `0.1.x` برای رفع آسیب‌پذیری‌های امنیتی پشتیبانی می‌شود.

## گزارش آسیب‌پذیری

لطفاً آسیب‌پذیری‌ها را در issue عمومی ثبت نکنید. یک گزارش private به نگه‌دارندگان repository ارسال کنید و شامل نسخهٔ آسیب‌پذیر، مراحل بازتولید، اثر مورد انتظار و در صورت امکان پیشنهاد اصلاح باشد. نگه‌دارندگان ظرف ۷ روز کاری دریافت گزارش را تأیید می‌کنند.

## مدل امنیتی Recipe

Recipe به‌صورت پیش‌فرض دادهٔ declarative است. Scafflare مسیرهای absolute، `..`، prefixهای Windows و مسیرهای خارج از sandbox را رد می‌کند. commandهای خارجی از Recipe فقط برای نمایش و اجرای صریح `--run-commands` نگه‌داری می‌شوند؛ commandهای string/shell-form قابل اجرا نیستند. Registry آنلاین و اجرای hook اسکریپتی در MVP وجود ندارد.
